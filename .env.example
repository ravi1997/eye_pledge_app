# ================================================================
# Eye Donation Pledge System - Environment Configuration
# ================================================================
# Copy this to .env and update with your values
# DO NOT commit .env to version control

# ================================================================
# Flask Configuration
# ================================================================
FLASK_ENV=development
FLASK_APP=app.py
DEBUG=False

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: Change this in production!
SECRET_KEY=dev-secret-key-change-in-production

# ================================================================
# Database Configuration
# ================================================================
# For SQLite (development):
# DATABASE_URL=sqlite:///eye_pledge.db

# For PostgreSQL (production):
# DATABASE_URL=postgresql://username:password@localhost:5432/eye_pledge_db

DATABASE_URL=sqlite:///eye_pledge.db

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production

# Institution Details
INSTITUTION_NAME=National Eye Bank
INSTITUTION_ADDRESS=Dr. RP Centre, AIIMS, New Delhi
INSTITUTION_PHONE=+91-11-XXXX-XXXX
INSTITUTION_EMAIL=eyebank@aiims.edu.in

# Application Settings
# ================================================================
# Number of pledges per page in admin list
PLEDGES_PER_PAGE=20
AUDIT_LOGS_PER_PAGE=50

# ================================================================
# Security Settings (Production)
# ================================================================
# Set to True only in production with HTTPS enabled
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# ================================================================
# Email Configuration (Optional - for future features)
# ================================================================
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_DEFAULT_SENDER=noreply@eyebank.org

# ================================================================
# Feature Flags
# ================================================================
# Enable/Disable experimental features
ENABLE_EMAIL_VERIFICATION=False
ENABLE_OTP_VERIFICATION=False
ENABLE_DIGITAL_SIGNATURE=False
ENABLE_API=False
ENABLE_EXPORT=True
ENABLE_PRINT=True

# ================================================================
# Notes
# ================================================================
# 1. For production deployment:
#    - Set SECRET_KEY to a strong random value
#    - Use PostgreSQL for DATABASE_URL
#    - Set SESSION_COOKIE_SECURE=True (with HTTPS)
#    - Configure email settings if needed
#    - Review security settings
#
# 2. To generate SECRET_KEY:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 3. Database setup:
#    flask db upgrade     # Apply migrations
#    flask create-admin   # Create first admin user
#
# 4. Run the app:
#    flask run
